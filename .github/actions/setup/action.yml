name: Setup
description: Setup Node.js and install dependencies

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: 20.x
        cache: pnpm
    - run: corepack enable
      shell: bash
    - run: pnpm install
      shell: bash
